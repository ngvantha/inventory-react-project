import { Typography } from "@mui/material"

const DefaultLayout = () => {
    return (
        <div>
            <Typography>Default Layout</Typography>
        </div>
    )
}

export default DefaultLayout